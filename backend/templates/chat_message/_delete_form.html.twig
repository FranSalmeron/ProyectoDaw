<form method="post" action="{{ path('app_chat_message_delete', {'chat': chat_message.chat}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chat_message.chat) }}">
    <button class="btn">Delete</button>
</form>
