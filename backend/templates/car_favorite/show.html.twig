{% extends 'base.html.twig' %}

{% block title %}CarFavorite{% endblock %}

{% block body %}
    <h1>CarFavorite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ car_favorite.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_car_favorite_index') }}">back to list</a>

    <a href="{{ path('app_car_favorite_edit', {'id': car_favorite.id}) }}">edit</a>

    {{ include('car_favorite/_delete_form.html.twig') }}
{% endblock %}
