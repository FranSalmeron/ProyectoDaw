<form method="post" action="{{ path('app_car_favorite_delete', {'id': car_favorite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ car_favorite.id) }}">
    <button class="btn">Delete</button>
</form>
